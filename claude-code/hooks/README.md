# Claude Code Hooks

향후 이 디렉토리에 Claude Code hooks 가 추가됩니다.

## 예정 hooks

- `PostToolUse` — 파일 편집 후 자동 포맷/린트
- `Stop` — 세션 종료 시 빌드 검증

## 설치 방법 (추후 업데이트)

```bash
bash scripts/install.sh --cc --hooks
```
