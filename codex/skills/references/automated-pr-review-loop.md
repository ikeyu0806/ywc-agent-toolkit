# Automated PR Review Loop

This reference is retained for backward compatibility with older `ywc-sequential-executor` text.

> **Action required**: Read [`../../references/pr-bot-polling.md`](../../references/pr-bot-polling.md) now. The canonical 60-second initial wait, 300-second polling window, `BOT_COUNT` jq query, `until` loop shape, and merge condition are maintained there.

Do not inline or approximate the polling logic here. Use the shared reference so sequential and parallel delivery flows remain aligned.
